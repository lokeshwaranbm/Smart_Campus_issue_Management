# Smart Campus Issue Management System

Project is split for future scalability:

- `frontend/` — React + Tailwind UI (Login/Sign Up flows)
- `backend/` — Express API scaffold for authentication and future database integration

## Run Frontend

```bash
cd frontend
npm install
npm run dev
```

## Run Backend

```bash
cd backend
npm install
npm run dev
```
