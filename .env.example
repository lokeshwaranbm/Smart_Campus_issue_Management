# Frontend API Configuration
# Set to your local backend URL (development)
VITE_API_BASE_URL=http://localhost:5000

# For production with local backend, use:
# VITE_API_BASE_URL=http://your-local-ip:5000
# or expose via ngrok and use: VITE_API_BASE_URL=https://your-ngrok-url.ngrok.io
